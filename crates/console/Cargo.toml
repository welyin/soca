[package]
name = "soca-console"
description = "SoCA 训练台：loopback-only 的 web 控制台（架构文档 §14、§8 的会话语义）"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
soca-contracts = { path = "../contracts" }
soca-core = { path = "../core" }
soca-core-actors = { path = "../core-actors" }
soca-model-gateway = { path = "../model-gateway" }
soca-storage = { path = "../storage" }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
uuid.workspace = true

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
